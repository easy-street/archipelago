{
  "$schema": "https://turborepo.dev/schema.json",
  "tasks": {
    "build": {
      "description": "Build a package or app for production. Outputs go to dist/ or .output/.",
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".output/**", ".vercel/output/**"]
    },
    "dev": {
      "description": "Run the local dev server (portless, HTTP-only, serves apps at http://<name>.localhost).",
      "cache": false,
      "persistent": true
    },
    "lint": {
      "description": "Lint source files with oxlint (never ESLint).",
      "dependsOn": ["^build"]
    },
    "typecheck": {
      "description": "Type-check with tsc --noEmit.",
      "dependsOn": ["^build"]
    },
    "test": {
      "description": "Run unit tests with Vitest.",
      "dependsOn": ["^build"]
    }
  }
}
