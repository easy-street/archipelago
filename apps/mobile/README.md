# mobile

Placeholder for the future React Native (Expo) app.

Intentionally empty — no `package.json` yet, so it is invisible to Bun workspaces and Turborepo until scaffolded. When the time comes, scaffold with Expo under Bun and consume shared packages from `packages/*`.
